# 修改配置数据

keywords: 修改,改,调整,设置为,改成,数值,属性

## 执行步骤

1. 从项目数据格式中确定目标配置文件
2. 从数据格式中获取字段列表和定位字段
3. 确定定位值（用户提到的对象名称）
4. 确定要修改的字段名和新值
5. 指定 old_value（当前值）用于安全校验
6. 输出 ConfigModifyPlan JSON

## 约束

- 必须指定 old_value，代码会校验当前值是否匹配
- 不改变文件中其他字段和其他记录
- 数值修改后应在合理范围内
- 如果涉及多条记录修改，逐条列出每个操作
